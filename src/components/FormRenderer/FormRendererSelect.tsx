import { FormFieldDefinition } from 'src/components/FormRenderer/types';

type FormRendererSelectProps = {
  formField: FormFieldDefinition;
};

const FormRendererSelect: FC<> = ({}) => {};

export default FormRendererSelect;
